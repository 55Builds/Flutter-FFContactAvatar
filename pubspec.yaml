name: ff_contact_avatar
description: Configurable widget for contact avatar, name and message, plus notification badge
version: 0.1.1
homepage: https://github.com/55Builds/Flutter-FFContactAvatar

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

